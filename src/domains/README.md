# Frontend Domains

Place product-specific frontend domains under `src/domains`.

Starter domains should stay minimal and demonstrate structure rather than real business features.

Current starter domains:

- `starter`
- `auth`
